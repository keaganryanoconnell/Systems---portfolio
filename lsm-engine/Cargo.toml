[package]
name = "lsm-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
common-protocol = { path = "../common-protocol" }
serde = { version = "1", features = ["derive"] }
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
