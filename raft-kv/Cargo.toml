[package]
name = "raft-kv"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A fault-tolerant key-value store coordinating mutations via the Raft consensus algorithm"

[dependencies]
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
